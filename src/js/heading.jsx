var React = require( "react" );

var Heading = React.createClass( {
  render: function() {
    return (
      <div className="heading">
        <h1>HEADER</h1>
      </div>
    );
  }
} );

module.exports = Heading;
